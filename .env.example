# Si tienes OpenClaw Gateway corriendo local:
GATEWAY_URL=http://127.0.0.1:18789/v1/chat/completions

# true = usar gateway para redactar/análisis; false = modo local (sin IA externa)
USE_GATEWAY=false

# timeouts (segundos)
GATEWAY_TIMEOUT=60

# Token del Gateway (si gateway.auth.mode="token")
OPENCLAW_GATEWAY_TOKEN=

# Carpeta de descargas del navegador (opcional)
DOWNLOADS_DIR=
